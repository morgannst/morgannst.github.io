/* ==========================================================================
 * ค่าตั้งต้นที่แก้ได้ "หลัง build" — ไม่ต้องคอมไพล์ใหม่
 * ==========================================================================
 *
 * ทำไมต้องมีไฟล์นี้
 * -----------------
 * Next.js แทนค่า NEXT_PUBLIC_* ลงในไฟล์ JS ตั้งแต่ตอน build แล้วตั้งชื่อไฟล์
 * ตามแฮชของเนื้อหา เช่น  _next/static/chunks/18-gjhkkm79p6.js
 *
 * แปลว่าถ้าแก้ .env แล้วลืมสั่ง build ใหม่ ค่าเดิมจะยังฝังอยู่ในไฟล์ที่อัปโหลดไป
 * อาการที่เจอคือ "กดลิงก์ LIFF แล้ววิ่งไปที่ endpoint เก่า" ทั้งที่แก้ .env แล้ว
 * ซึ่งหาสาเหตุยากมาก เพราะหน้าเว็บทำงานได้ปกติทุกอย่าง
 *
 * ไฟล์นี้ถูกโหลดตอน runtime จึงแก้แล้วรีเฟรชเห็นผลทันที
 *
 * วิธีใช้
 * -------
 * ปกติ "ไม่ต้องแก้อะไรเลย" — เว้นทุกช่องว่างไว้ ระบบจะใช้ค่าจาก .env ที่ build มา
 * จะกรอกก็ต่อเมื่อต้องการทับค่าที่ build มาแล้ว เช่น
 *   • ย้าย hosting แล้วต้องเปลี่ยนไปใช้ LIFF app ตัวใหม่
 *   • แจกไฟล์ที่ build แล้วให้คนอื่นเอาไปตั้งค่าเอง
 *   • ทดสอบชี้ไปฐานข้อมูลอีกตัวชั่วคราว
 *
 * ⚠️ ค่าในไฟล์นี้เปิดเผยต่อสาธารณะเหมือนกับ NEXT_PUBLIC_* ทุกประการ
 *    ห้ามใส่ service role key หรือความลับใด ๆ เด็ดขาด
 * ========================================================================== */

window.__APP_CONFIG__ = {
  // LIFF ID ของแอปที่ endpoint ชี้มายังเว็บชุดนี้
  // (ดูได้ที่ LINE Developers Console → LINE Login channel → LIFF)
  LIFF_ID: '',

  // Supabase ของโปรเจกต์นี้ (ใช้ anon key เท่านั้น)
  SUPABASE_URL: '',
  SUPABASE_KEY: ''
};
